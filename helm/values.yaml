# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: rdlsolutions/hobot
  tag: "v1.0.9-5e22e32"
  arch: amd64
secret:
  name: "hobot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true